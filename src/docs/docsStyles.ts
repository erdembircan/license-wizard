// Loaded by the documentation pages in dev (see scripts/vite-docs-plugin.mjs)
// so Vite serves the site stylesheet with hot-module reloading. In production
// the prerendered pages link the built stylesheet directly instead.
import "../style.css";
